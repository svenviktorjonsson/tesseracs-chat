# Start from the official Python image
FROM python:3.11-slim

# Install uv, then use uv to install the plotting libraries into the system environment
RUN pip install uv && uv pip install --system matplotlib mpld3